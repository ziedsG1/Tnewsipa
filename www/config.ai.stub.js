/** Default AI config (no key). Overridden by config.ai.js when you build locally. */
window.TNEWS_AI_CONFIG = {
  apiKey: "",
  baseUrl: "https://api.openai.com/v1/chat/completions",
  model: "gpt-4o-mini",
};
