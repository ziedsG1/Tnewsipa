/** Copy to secrets.local.js (gitignored) — never commit the real file. */
window.TnewsAiSecrets = {
  apiKey: "sk-your-key-here",
  baseUrl: "https://api.openai.com/v1/chat/completions",
  model: "gpt-4o-mini",
};
